import React, { useState, useEffect } from 'react';
import {
  View,
  Text,
  StyleSheet,
  FlatList,
  SafeAreaView,
  TouchableOpacity,
  Alert,
} from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { Trash2, Calendar, TrendingUp } from 'lucide-react-native';
import HistoryItem from '@/components/HistoryItem';
import { getWinePredictionHistory, clearWinePredictionHistory } from '@/utils/storage';
import type { WinePrediction } from '@/types/wine';

export default function HistoryScreen() {
  const [history, setHistory] = useState<WinePrediction[]>([]);

  useEffect(() => {
    loadHistory();
  }, []);

  const loadHistory = async () => {
});
